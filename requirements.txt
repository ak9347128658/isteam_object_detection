# ============================================================================
# Product Detection & Recommendation Pipeline - Python dependencies
# ----------------------------------------------------------------------------
# GPU note: install a CUDA build of torch that matches your driver, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then: pip install -r requirements.txt
# ============================================================================

# --- Core CV / detection ---
ultralytics>=8.3.0          # YOLOE / YOLO-World open-vocabulary detection
opencv-python-headless>=4.9.0
numpy>=1.26.0
pillow>=10.2.0
imagehash>=4.3.1            # perceptual hashing for frame/crop dedup

# --- Embeddings for dedup + offline matching ---
open-clip-torch>=2.24.0
torch>=2.2.0               # install the CUDA build separately (see note above)
torchvision>=0.17.0

# --- Video / IO ---
yt-dlp>=2024.3.10          # robust video download from URLs
requests>=2.31.0
tqdm>=4.66.0

# --- AWS S3 ---
boto3>=1.34.0

# --- Search backends ---
google-search-results>=2.4.2   # official SerpApi python client

# --- Config / notebook ---
pyyaml>=6.0.1
python-dotenv>=1.0.1
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0          # optional: preview detections in the notebook
